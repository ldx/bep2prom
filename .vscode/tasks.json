{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build //cmd/bep2prom",
            "type": "shell",
            "command": "bazel build --strip=never -c dbg //cmd/bep2prom"
        }
    ]
}
